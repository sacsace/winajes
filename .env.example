# Local development — PostgreSQL 17 (port 5432 on this PC)
DATABASE_URL=postgresql://winajes:winajes_dev_password@localhost:5432/winajes

PGHOST=localhost
PGPORT=5432
DB_USER=winajes
DB_PASSWORD=winajes_dev_password
DB_NAME=winajes

# One-time DB setup (postgres superuser password for npm run db:init)
POSTGRES_ADMIN_PASSWORD=

NEXT_PUBLIC_SITE_URL=http://localhost:3000
